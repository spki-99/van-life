import { styled } from 'styled-components';

const Container = styled.section`
    margin-top: 30px;
    display: flex;
    gap: 20px;
    overflow: auto;
    
    img {
        width: 100px;
        border-radius: 5px;
    }
`
export default Container;